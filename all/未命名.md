```mermaid

flowchart TB
  %% === 主流程 ===
  A([开始]) --> B[加载系统参数配置]
  B --> C[加载模型基座]
  C --> D[初始化训练环境]
  D --> E[初始化网络资源]
  E --> F[预置提示词模板]
  F --> G[设置权限与角色]
  G --> H[生成默认配置模板]
  H --> I([结束])

  %% === 辅助模块 ===
  B -.-> J[读取用户选择的引接类型 (API/JDBC/Excel/消息队列)]
  H -.-> K[输出默认配置模板参数]

```