```mermaid
%%{init: {
  "flowchart": { "curve": "linear" },
  "themeVariables": {
    "primaryColor": "#ffffff",
    "edgeLabelBackground": "transparent",
    "fontSize": "14px"
  },
  "themeCSS": ".edgeLabel rect { fill: transparent !important; stroke: transparent !important; }"
}}%%
flowchart TB
  %% === 主流程 ===
  A([开始]) --> B[加载系统参数配置]
  B --> C[加载模型基座]
  C --> D[初始化训练环境]
  D --> E[初始化网络资源]
  E --> F[预置提示词模板]
  F --> G[设置权限与角色]
  G --> H[生成默认配置模板]
  H --> I([结束])

  %% === 辅助模块 ===
  B -.-> J[读取用户选择的引接类型 (API/JDBC/Excel/消息队列)]
  H -.-> K[输出默认配置模板参数]

  %% === 样式定义 ===
  classDef ellipse fill:#ffffff,stroke:#000,stroke-width:1px,rx:25,ry:25;
  classDef rect fill:#ffffff,stroke:#000,stroke-width:1px;
  classDef dashed fill:#ffffff,stroke:#000,stroke-dasharray:4 4;
  class A,I ellipse;
  class B,C,D,E,F,G,H rect;
  class J,K dashed;

```